<div class="group">
  <h3 class="inline-block px-3 py-2 bg-orange text-white font-bold text-2xl">
    {{ title }}
  </h3>

  <div class="border-2 border-orange bg-white">
    {% if image_src %}
      <img class="block w-full object-cover" src="{{ image_src }}" height="320" alt="">
    {% endif %}

    {% if text %}
      <div class="wysiwyg m-4">
        {{ text }}
      </div>
    {% endif %}

    {% if link %}
      <div class="m-4">
        <a class="Block Block--light" href="{{ link }}">See {{ title }}</a>
      </div>
    {% endif %}
  </div>
</div>
